<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.loc2.loc2me"
    android:versionCode="102"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <uses-feature android:name="android.hardware.wifi" />

    <application
        android:name="me.loc2.loc2me.BootstrapApplication"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.Bootstrap.Dark">

        <activity
            android:name="me.loc2.loc2me.ui.MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name="me.loc2.loc2me.ui.BootstrapTimerActivity"
                  android:configChanges="orientation|keyboardHidden|screenSize"
                  android:label="@string/app_name"
                  android:launchMode="singleTop"
                  android:parentActivityName="me.loc2.loc2me.ui.MainActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <!-- Both parent activity attributes have to contain the full package name -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="me.loc2.loc2me.ui.MainActivity" />
        </activity>

        <service android:name="me.loc2.loc2me.core.TimerService" android:enabled="true" android:exported="false" />
        <service android:name="me.loc2.loc2me.core.WifiScanService" android:enabled="true" android:exported="false" />
        <service android:name="me.loc2.loc2me.core.OfferLoaderService" android:enabled="true" android:exported="false" />
        <service android:name="me.loc2.loc2me.core.OfferStorageService" android:enabled="true" android:exported="false" />

        <service
            android:name="me.loc2.loc2me.authenticator.AccountAuthenticatorService"
            android:process=":auth" >
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>

        <activity
            android:name="me.loc2.loc2me.authenticator.BootstrapAuthenticatorActivity"
            android:excludeFromRecents="true" >

            <!--
                No intent-filter here! This activity is only ever launched by
                someone who explicitly knows the class name
            -->
        </activity>

        <activity android:name="me.loc2.loc2me.ui.OfferActivity" />
        <activity android:name="me.loc2.loc2me.ui.UserActivity" android:label="@string/app_name" />

    </application>

</manifest>
